[pytest]
# ===========================================
# Pytest Configuration
# ===========================================

# Test discovery
python_files = test_*.py *_test.py
python_classes = Test*
python_functions = test_*
testpaths = tests

# Minimum Python version
minversion = 7.0

# CLI options
# Note: Coverage flags are NOT included here to avoid overhead on local
# subset runs and conflicts with CI explicit coverage commands.
# Use `pytest --cov=shared --cov=services --cov=server` for local coverage.
addopts =
    # Verbose output
    -v
    # Show local variables in tracebacks
    --showlocals
    # Show summary of all test outcomes
    -ra
    # Shorter traceback format
    --tb=short
    # Strict markers (fail on unknown markers)
    --strict-markers
    # Show warnings
    -W default

# Custom markers for test categorization
markers =
    unit: Unit tests (fast, no external dependencies)
    integration: Integration tests (may need database)
    slow: Slow-running tests (>1 second)
    smoke: Smoke tests for CI/CD health checks
    api: API endpoint tests
    database: Database-dependent tests
    pipeline: Data pipeline tests
    embeddings: Embedding generation tests (slow, requires models)
    llm: Tests requiring LLM API calls (may cost money)
    docker: Tests requiring Docker containers

# Environment variables for test isolation
env =
    TESTING=1
    SQL_ECHO=false

# Asyncio configuration
asyncio_mode = auto

# Ignore paths
norecursedirs =
    .git
    .github
    __pycache__
    htmlcov
    client/node_modules
    venv
    alembic
    docker

# Log configuration
log_cli = false
log_cli_level = INFO
log_cli_format = %(asctime)s [%(levelname)s] %(message)s
log_cli_date_format = %Y-%m-%d %H:%M:%S

# Timeout for tests (prevent hanging)
timeout = 300
timeout_method = thread
